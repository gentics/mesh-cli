{
  "name": "mesh-cli",
  "version": "0.0.4",
  "description": "A command line interface for Gentics Mesh, an API-first CMS",
  "main": "./build/mesh-cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/tsc -p ."
  },
  "author": "Clemens Prerovsky <c.prerovsky@gentics.com>",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^7.0.5",
    "mesh-api": "^0.0.14",
    "text-table": "^0.2.0"
  },
  "repository": {
    "url": "git@github.com:gentics/mesh-cli.git"
  },
  "bin": {
    "mesh-cli": "./build/mesh-cli.js"
  },
  "devDependencies": {
    "typescript": "^2.1.6"
  }
}
